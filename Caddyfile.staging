# Caddyfile.staging — HTTP sem TLS, para testes por IP antes do DNS
# Uso: docker-compose -f docker-compose.yml -f docker-compose.staging.yml up -d

:80 {
	handle_path /api/* {
		reverse_proxy api:3000
	}

	handle {
		reverse_proxy web:80
	}

	encode gzip
}
